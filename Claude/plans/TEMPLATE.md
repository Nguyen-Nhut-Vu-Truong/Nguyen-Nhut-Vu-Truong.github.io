# Plan: <short title>

<!--
  How to use this file:
    1. Copy it to Claude/plans/<name>.md
    2. Fill in every section below
    3. Commit and push to main BEFORE starting the cloud session
  The session clones from GitHub, so an unpushed plan is invisible to it.
-->

## Goal

One or two sentences. What should be true when this is finished?

## Out of scope

What should *not* be touched. Be explicit — this is the section that prevents
unwanted changes while nobody is watching.

## Steps

1.
2.
3.

## Acceptance criteria

How to tell the work is actually done. Prefer checks that can be run.

- [ ]
- [ ]

## Constraints

- Do not modify: (list files that must stay untouched)
- Do not open a pull request unless this plan says to.
- Stop and leave a note in the session instead of guessing if a step is
  ambiguous.

## Done means

- Each step committed separately, with a message explaining why.
- All commits pushed to branch: `claude/<branch-name>`
- A short summary written to `Claude/plans/<name>-result.md` covering what was
  done, what was skipped, and anything that needs a decision.
